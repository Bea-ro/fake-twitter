import { HighlightTweetDirective } from './highlight-tweet.directive';

describe('HighlightTweetDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTweetDirective();
    expect(directive).toBeTruthy();
  });
});
